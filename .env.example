# Baza danych (SQLite do demo — plik lokalny)
DATABASE_URL="file:./dev.db"

# Klucze Stripe w trybie TESTOWYM (Dashboard Stripe -> Developers -> API keys)
# Bez tych kluczy checkout działa w trybie symulowanym (zamówienie od razu "opłacone").
STRIPE_SECRET_KEY=""
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=""

# Klucz dostępu do panelu zamówień: dotczak.pl/admin/orders?key=...
# Zostaw puste, aby panel był dostępny bez hasła (tylko do demo).
ADMIN_KEY=""
